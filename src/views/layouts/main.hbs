<!DOCTYPE html>
<html lang="en">

<head>
    {{!-- HEADER GENERAL --}}
    {{> header }}

    <!-- DASHBOARD USUARIO EMPRESA -->
    {{#if user_dash}}
        {{> user/headerUser }}
    {{/if}}
    {{!-- DASHBOARD ADMINISTRADOR --}}
    {{#if adminDash}}
        {{> admin/headerAdmin }}
    {{/if}}

</head>

<body class="barra-scroll overflow-y-auto">
    {{!-- INICIO DE LA ETIQUETA DIV WRAPPER --}}
    {{#if user_dash}}
        {{> user/navigation }}
    {{/if}}
    {{#if adminDash}}
        {{> admin/navigationAdmin }}
    {{/if}}
    
    
    {{{ body }}}

    </div>
    <!-- ./FIN DEL MAIN WRAPPER -->
    

    {{!-- FOOTER PARTIALS PRINCIPAL --}}
    {{> footer }}
    {{!-- FOOTER USUARIO EMPRESA --}}
    {{#if user_dash}}
        {{> user/footerUser }}
    {{/if}}
    {{!-- FOOTER ADMINISTRADOR --}}
    {{#if adminDash}}
        {{> admin/footerAdmin }}
    {{/if}}

    {{!-- SCRIPTS PARA EL LOGIN --}}
    {{#if confirmarLogin}}
    <script>
        $(document).ready(function () {
        });
        
        (function ($) {
            jQuery(document).ready(function () {
            });

            jQuery(window).on('load', function () {
                Swal.fire({
                    icon: 'success',
                    title: 'Correo verificado',
                    text: 'Has confirmado satisfactoriamente tu cuenta, ahora puedes iniciar sesión.',
                })
            });

        })(jQuery);
    </script>
    {{/if}}

</body>

</html>